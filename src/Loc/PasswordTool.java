/*
package Loc;

public class PasswordTool {
    public static boolean isPasswordValid(String password) {
        boolean isPasswordLongerThan8 = isPasswordLongerThan8(password);
        boolean doesPasswordContainNumber = doesPasswordContainNumber(password);
        boolean doesPasswordContainUppercase = doesPasswordContainNumber(password);
        return  isPasswordLongerThan8 && doesPasswordContainNumber && doesPasswordContainUppercase;
    }

   */
/* private static boolean doesPasswordContainNumber(String password) {
        char
    }
*//*

    private static boolean isPasswordLongerThan8(String password) {
        if (password.length()>=8) return true;
        else return false;  
    }
}
*/
